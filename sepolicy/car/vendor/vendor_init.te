# allow vendor_init debugfs:file write;
# allow vendor_init default_prop:file read;
allow vendor_init proc_sched:file write;
# allow vendor_init system_data_file:dir { setattr write };
allow vendor_init system_prop:file read;
allow vendor_init vehicle_device:file create;
